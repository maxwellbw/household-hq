# The deployed Apps Script web-app URL (features 001-005 backend).
VITE_API_BASE_URL=https://script.google.com/macros/s/REPLACE_ME/exec

# Google OAuth Web client ID — must match backend/Config.js OAUTH_CLIENT_ID exactly.
VITE_GOOGLE_CLIENT_ID=REPLACE_ME.apps.googleusercontent.com
